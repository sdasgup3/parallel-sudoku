mainmodule Module {
  readonly  CProxy_Main mainProxy;
  readonly  AdjListType adjList_;
  readonly  int vertices_;

mainchare Main {
    entry Main(CkArgMsg*);
    entry [reductiontarget] void done(void);
  };
  
  chare Node { 
    entry Node();
    entry void testGraph(std::vector<State>& );
  };
};
